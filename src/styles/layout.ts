/**
 * The page shell.
 *
 * Every top-level band — navbar, hero, upload,
 * loading, report, error, footer — sits on this
 * measure and these gutters, so the content edge
 * never moves as the user scrolls.
 *
 * Change it here and it changes everywhere.
 */
export const SHELL = "mx-auto w-full max-w-[1320px] px-5 sm:px-7 lg:px-10";

/**
 * Clearance for the sticky navbar, which is
 * h-[58px]. Used by every section that is a
 * scrollIntoView target.
 */
export const SCROLL_CLEARANCE = "scroll-mt-[58px]";
